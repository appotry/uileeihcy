

2013.6.6
开始对列表型控件进行再次优化，具体任务有：
1). 将listctrlbase, listitembase挪到UISDK工程中，将其作为基本类
2). 重新梳理listctrl与item的关系，立争一个listctrl只作为一个容器和接口对象，item可以插入任意一种listctrl容器中。例如菜单中可以支持任意item type。
3). 对于itemtypesharedata这一期先不做太高的期望


2013.3
升级版本为0.8
开始重写整个UI库的架构，将UI库能够分隔成多个部分，Kernel + Ctrl + 模块控件，同时保证控件类能够跨模块继承

0.7
已实现了PLAY_UI列表型控件及PLAY_SOUND模块，但在跨模块之间继承时容易发生问题。因为该版本中是将整个控件类完全导出实现的，类中的String成员也被导致，导致跨模块时极易发生问题

0.6
将UIApplication全局类修改为接口类，支持多个实例存在。用于支持UIEditor中即要编辑皮肤，又要自己使用皮肤的问题


后面由于代码版本多次在本地SVN服务器/googlecode SVN服务器之间切换，对版本号进行了若干次修改

2012.6.15
0.4
一个终于能看到PLAYER_UI原型的版本，但还没有实现滚动条

2009.11
0.1 
开始模拟MSN的DirectUI登录界面，将控件布局写在代码中以防用户修改（后来觉得这样太不灵活了）